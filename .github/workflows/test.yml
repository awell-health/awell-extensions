name: Compile and test

on:
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - run: yarn
    - name: Compile Typescript
      run: yarn build
    - name: Run tests
      run: yarn test