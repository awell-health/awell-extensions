query getFormTemplate($id: ID) {
  customModuleForm(id: $id) {
    id
    name
    use_for_charting
    use_for_program
    prefill
    has_matrix_field
    is_video
    has_non_readonly_modules
    custom_modules {
      id
      mod_type
      label
    }
  }
}
