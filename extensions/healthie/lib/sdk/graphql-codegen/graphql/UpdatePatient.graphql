mutation updatePatient($input: updateClientInput!) {
  updateClient(input: $input) {
    user {
      id
      first_name
      last_name
      legal_name
      email
      quick_notes
    }
    messages {
      field
      message
    }
  }
}
