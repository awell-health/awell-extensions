# Base API URL for the server (no credentials in URL)
VITE_API_URL=http://localhost:5057/api

# Set allowed host when exposing Vite dev server with a tunnel
ALLOWED_HOST=your-tunnel-host.dev

# Optional: basic auth for API, format user:password
# When set, the client will send Authorization: Basic &lt;base64(user:password)&gt;
VITE_API_BASIC_AUTH=
